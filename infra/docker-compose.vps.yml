# VPS overlay — bind API to localhost only (use with Cloudflare Tunnel on the same host).
#
#   docker compose -f docker-compose.prod.yml -f docker-compose.vps.yml up -d --build

services:
  api:
    ports:
      - "127.0.0.1:${API_PORT:-3000}:3000"
