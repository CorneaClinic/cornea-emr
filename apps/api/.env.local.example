# Local PostgreSQL for backup restore drills — DO NOT COMMIT.
# Copy to .env.local and set your local postgres/cornea credentials.
# Used when production is on DigitalOcean managed Postgres (no CREATEDB from clinic PC).
#
DATABASE_URL=postgres://cornea:REPLACE_ME@127.0.0.1:5432/postgres
